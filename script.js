// create dropdown menu
const hamburger = document.querySelector('.hamburger');
const navLinks = document.querySelector('.nav-links');
dropDown(hamburger, navLinks);

document.querySelector('#start').addEventListener('click', ()=>{
    init(displayData)
});